namespace ODExplorer.Models
{
    // Minimal cross-platform visibility enum to replace System.Windows.Visibility in core
    public enum Visibility
    {
        Visible,
        Collapsed,
        Hidden
    }
}
